<?php include 'db.php'; ?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>NEECCAA - Shop Online Like Amazon & Jumia</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: #f8f9fa; }
        .navbar { background: linear-gradient(45deg, #0d6efd, #0dcaf0); }
        .logo { font-size: 28px; font-weight: bold; color: white; }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <a class="navbar-brand logo" href="index.php">NEECCAA</a>
        <form class="d-flex mx-auto w-50" method="GET">
            <input class="form-control me-2" type="search" name="search" placeholder="Search products..." required>
            <button class="btn btn-warning" type="submit">Search</button>
        </form>
        <div>
            <?php if(isset($_SESSION['user_id'])): ?>
                <a href="cart.php" class="btn btn-light me-2">Cart</a>
                <a href="logout.php" class="btn btn-outline-light">Logout</a>
            <?php else: ?>
                <a href="login.php" class="btn btn-outline-light me-2">Login</a>
                <a href="register.php" class="btn btn-light">Register</a>
            <?php endif; ?>
            <?php if(isset($_SESSION['role']) && $_SESSION['role']=='admin'): ?>
                <a href="admin/" class="btn btn-danger ms-2">Admin Panel</a>
            <?php endif; ?>
        </div>
    </div>
</nav>

<div class="container mt-5">
    <div class="alert alert-info text-center">
        <h1>Welcome to <strong>NEECCAA</strong> – Your No.1 Online Store!</h1>
    </div>

    <div class="row">
        <?php
        $sql = "SELECT * FROM products WHERE stock > 0";
        if(isset($_GET['search'])) {
            $s = mysqli_real_escape_string($conn, $_GET['search']);
            $sql .= " AND name LIKE '%$s%'";
        }
        $sql .= " ORDER BY created_at DESC";
        $res = mysqli_query($conn, $sql);
        while($p = mysqli_fetch_assoc($res)):
        ?>
        <div class="col-md-3 mb-4">
            <div class="card h-100 shadow">
                <img src="admin/uploads/<?= $p['image'] ?>" class="card-img-top" height="220">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title"><?= htmlspecialchars($p['name']) ?></h5>
                    <p class="text-success fw-bold fs-4">₦<?= number_format($p['price']) ?></p>
                    <p>Stock: <?= $p['stock'] ?></p>
                    <a href="add_to_cart.php?id=<?= $p['id'] ?>" class="btn btn-primary mt-auto">Add to Cart</a>
                </div>
            </div>
        </div>
        <?php endwhile; ?>
    </div>
</div>
</body>
</html>
